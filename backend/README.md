---
title: GEX Terminal API
emoji: 📈
colorFrom: blue
colorTo: gray
sdk: docker
app_port: 7860
---

Backend FastAPI del GEX Quant Terminal (migración desde Streamlit). Ver el
plan completo del proyecto en `docs/` del repo raíz.

Este Space se actualiza automáticamente desde GitHub (workflow
`.github/workflows/sync-to-hf.yml`) — no editar directamente en el git de
Hugging Face, los cambios se sobreescriben en el próximo push a `main`.
